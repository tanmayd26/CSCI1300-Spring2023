#include <iostream>

using namespace std;

int main()
{
    string my_str = "As People Walk This Way Again and Again, a Path Appears";

    // LetsRemoveAllTheSpaces
    // GETTING TO THE PART OF CLASS WHERE PSEUDOCODE IS ACTUALLY HELPFUL
    // I always start by doing it myself

    // go character by char
    // check to see if the char is ' '
    // if it is, skip it
    // otherwise, write it down (in a new string)
    ______ compressed_str = "";

    for(int i = 0; _____________________; i++)
    {
        if (______________)
        {
            compressed_str _____________
        }
    }

    cout << "The new string is " << compressed_str << endl;
    
    return 0;
}